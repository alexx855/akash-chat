/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // output: process.env.STATIC_OUTPUT ? 'export' : undefined,
  // distDir: 'extension/popup',
  // assetPrefix: process.env.STATIC_OUTPUT ? '' : '/.',
  // trailingSlash: true,
};
module.exports = nextConfig;
